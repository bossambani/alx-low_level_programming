#include "main.h"
/**
 * main - check if c is uppercase or not.
 *
 * Return 1 if c is uppercase.
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

}

