#include "main.h"
#include <string.h>

/**
 * _strlen - function for cheking the length of a string
 * @s: My pointer variable
 * Return: Always (0).
 */
int _strlen(char *s)
{
	return (strlen(s));
}
